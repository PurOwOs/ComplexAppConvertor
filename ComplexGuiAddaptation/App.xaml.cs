using Microsoft.Maui.Controls;

namespace ComplexGuiAddaptation  // Ensure this matches the project namespace
{
    public class App : Application
    {
        public App()
        {
            MainPage = new MainPage();  // Ensure MainPage is referenced correctly
        }
    }
}
